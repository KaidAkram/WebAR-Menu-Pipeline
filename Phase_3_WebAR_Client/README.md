# Phase 3: WebAR Client
This folder is strictly reserved for the WebAR frontend code (SuperSplat / PlayCanvas integration).

The web development team will push the final `index.html` and viewing logic here to load the optimized `.ply` models.
